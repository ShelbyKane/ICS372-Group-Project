# ICS372 Group Project – Order Management System (JavaFX)

## 📌 Overview
This project is a JavaFX-based Order Management System built using Gradle. It supports order creation, management, and UI-based interaction through a JavaFX interface.
This ReadMe was generated with the help of ChatGPT
---

## ⚙️ Requirements

Before running the project, make sure you have:

- Java JDK 22 (or compatible JDK 17+)
- Gradle (or use the included Gradle Wrapper)
- Internet connection (first run downloads dependencies)

---

## 🚀 How to Run the Project

### Put order files in downloadedOrders folder to import

### ▶️ Option 1: Run using Gradle (Recommended)

From the **project root directory** (where `build.gradle` is located), run:

```bash
.\gradlew run

